export type PartnerType = {
    id: number,
    name: string,
    username: string,
    password: string,
    createdAt: Date,
    updatedAt: Date,
}

export type LoginPartnerType = {
    username: string,
    password: string
}